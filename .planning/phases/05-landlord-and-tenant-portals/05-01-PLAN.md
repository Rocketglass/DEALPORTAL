---
phase: 05-landlord-and-tenant-portals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/portal-sidebar.tsx
  - src/app/(landlord)/landlord/layout.tsx
  - src/app/(tenant)/tenant/layout.tsx
autonomous: true
requirements: [LAND-04, TNNT-01]
must_haves:
  truths:
    - "Landlord route group exists with auth-guarded layout and role-specific sidebar"
    - "Tenant route group exists with auth-guarded layout and role-specific sidebar"
    - "Both layouts use the same visual language as the broker portal (Stripe-like)"
    - "Sign out works from both portals"
  artifacts:
    - path: "src/components/layout/portal-sidebar.tsx"
      provides: "Role-aware sidebar component accepting nav items and portal prefix"
      exports: ["PortalSidebar"]
    - path: "src/app/(landlord)/landlord/layout.tsx"
      provides: "Landlord portal layout with auth guard and sidebar"
      contains: "requireLandlordOrAgent"
    - path: "src/app/(tenant)/tenant/layout.tsx"
      provides: "Tenant portal layout with auth guard and sidebar"
      contains: "requireTenantOrAgent"
  key_links:
    - from: "src/app/(landlord)/landlord/layout.tsx"
      to: "src/lib/security/auth-guard.ts"
      via: "requireLandlordOrAgent()"
      pattern: "requireLandlordOrAgent"
    - from: "src/app/(tenant)/tenant/layout.tsx"
      to: "src/lib/security/auth-guard.ts"
      via: "requireTenantOrAgent()"
      pattern: "requireTenantOrAgent"
    - from: "src/app/(landlord)/landlord/layout.tsx"
      to: "src/components/layout/portal-sidebar.tsx"
      via: "PortalSidebar component import"
      pattern: "PortalSidebar"
---

<objective>
Create the shared layout infrastructure for landlord and tenant portals -- a reusable role-aware sidebar component and authenticated layout files for both route groups.

Purpose: Both portals need the same visual language (sidebar + header + main content area) as the broker portal, but with role-specific navigation items and auth guards. Building this shared infrastructure first lets Plans 02 and 03 focus purely on page content.

Output: PortalSidebar component, (landlord) and (tenant) route group layouts
</objective>

<execution_context>
@/Users/neilbajaj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neilbajaj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-landlord-and-tenant-portals/05-CONTEXT.md

@src/components/layout/sidebar.tsx
@src/components/layout/header.tsx
@src/components/layout/mobile-nav.tsx
@src/app/(portal)/layout.tsx
@src/lib/security/auth-guard.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/security/auth-guard.ts:
```typescript
export interface AuthUser {
  id: string;
  email: string;
  role: UserRole;
  contactId: string | null;
  principalId: string | null;
}
export async function requireLandlordOrAgent(): Promise<AuthUser>;
export async function requireTenantOrAgent(): Promise<AuthUser>;
```

From src/types/database.ts:
```typescript
export type UserRole = 'admin' | 'broker' | 'tenant' | 'landlord' | 'tenant_agent' | 'landlord_agent';
```

Existing sidebar navItems pattern (src/components/layout/sidebar.tsx):
```typescript
const navItems = [
  { href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/properties', label: 'Properties', icon: Building2 },
  // ...
];
```

Existing portal layout pattern (src/app/(portal)/layout.tsx):
- Server component with auth guard call
- Renders Sidebar (hidden on mobile) + MobileNav + Header + main content
- Uses flex layout, min-h-screen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PortalSidebar -- a reusable role-aware sidebar component</name>
  <files>src/components/layout/portal-sidebar.tsx</files>
  <read_first>src/components/layout/sidebar.tsx, src/components/layout/mobile-nav.tsx</read_first>
  <action>
Create a new `PortalSidebar` component that accepts nav items and a portal base path as props, reusing the exact same visual design as the existing broker `Sidebar` component.

Props interface:
```typescript
interface PortalSidebarProps {
  navItems: { href: string; label: string; icon: LucideIcon }[];
  portalName: string; // e.g. "Landlord Portal", "Tenant Portal"
}
```

The component must:
1. Accept navItems array and portalName as props (NOT hardcode nav items)
2. Use identical styling to existing Sidebar: same width (248px), same border-r, same bg-[var(--background-raised)], same nav item styling with active indicator pill, same font sizes (13px for nav, 11px for email)
3. Include the brand header (Rocket Realty logo + portalName subtitle in 11px muted text below "Rocket Realty")
4. Include the user info section at bottom (initials avatar, name, email) -- fetch from supabase.auth.getUser() same as existing Sidebar
5. Include Settings link (href to /landlord/settings or /tenant/settings -- derive from first navItem's prefix) and Sign Out button with same handler pattern
6. Mark as 'use client' (uses usePathname, useEffect, useState)
7. isActive check: `pathname.startsWith(item.href)` same as broker sidebar

Do NOT duplicate the mobile nav -- that will be handled separately. Do NOT include "View Public Site" link (not relevant for landlord/tenant portals).
  </action>
  <verify>
    <automated>cd /Users/neilbajaj/SersweAI/Client\ work/Rocket\ Realty/rocket-realty-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function PortalSidebar" src/components/layout/portal-sidebar.tsx
    - grep -q "portalName" src/components/layout/portal-sidebar.tsx
    - grep -q "navItems" src/components/layout/portal-sidebar.tsx
    - grep -q "'use client'" src/components/layout/portal-sidebar.tsx
    - grep -q "usePathname" src/components/layout/portal-sidebar.tsx
    - grep -q "handleSignOut" src/components/layout/portal-sidebar.tsx
  </acceptance_criteria>
  <done>PortalSidebar component exists, accepts navItems and portalName props, compiles without errors, matches broker sidebar visual design</done>
</task>

<task type="auto">
  <name>Task 2: Create landlord and tenant portal layouts with auth guards</name>
  <files>src/app/(landlord)/landlord/layout.tsx, src/app/(tenant)/tenant/layout.tsx</files>
  <read_first>src/app/(portal)/layout.tsx, src/components/layout/portal-sidebar.tsx</read_first>
  <action>
Create two layout files following the exact pattern of the existing broker portal layout (src/app/(portal)/layout.tsx).

**Landlord layout** at `src/app/(landlord)/landlord/layout.tsx`:
1. Server component with `export const dynamic = 'force-dynamic'`
2. Call `requireLandlordOrAgent()` from `@/lib/security/auth-guard` -- if the function redirects, the user never sees the layout (this is the existing pattern, broker layout does equivalent with manual check)
3. Also allow broker/admin access: after requireLandlordOrAgent fails, catch the redirect and try requireAuth() -- if user is broker/admin, let them through. Actually, simpler: use `requireRole('landlord', 'landlord_agent', 'broker', 'admin')` since brokers can access all portals per STATE.md decision.
4. Import PortalSidebar and render with landlord nav items:
   - { href: '/landlord/dashboard', label: 'Dashboard', icon: LayoutDashboard }
   - { href: '/landlord/applications', label: 'Applications', icon: FileText }
   - { href: '/landlord/properties', label: 'Properties', icon: Building2 }
5. portalName: "Landlord Portal"
6. Include Header component and MobileNav (same pattern as broker layout)
7. Same flex layout structure: sidebar hidden on lg, mobile nav shown on mobile, header, main with id="main-content"
8. Include skip-to-content link (accessibility, same as broker layout)

**Tenant layout** at `src/app/(tenant)/tenant/layout.tsx`:
1. Same server component pattern
2. Use `requireRole('tenant', 'tenant_agent', 'broker', 'admin')` for auth guard
3. Tenant nav items:
   - { href: '/tenant/dashboard', label: 'Dashboard', icon: LayoutDashboard }
   - { href: '/tenant/applications', label: 'My Applications', icon: FileText }
4. portalName: "Tenant Portal"
5. Same layout structure as landlord

Both layouts must create the route group directories: `(landlord)/landlord/` and `(tenant)/tenant/`.
  </action>
  <verify>
    <automated>cd /Users/neilbajaj/SersweAI/Client\ work/Rocket\ Realty/rocket-realty-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "requireRole" src/app/\(landlord\)/landlord/layout.tsx
    - grep -q "PortalSidebar" src/app/\(landlord\)/landlord/layout.tsx
    - grep -q "landlord/dashboard" src/app/\(landlord\)/landlord/layout.tsx
    - grep -q "requireRole" src/app/\(tenant\)/tenant/layout.tsx
    - grep -q "PortalSidebar" src/app/\(tenant\)/tenant/layout.tsx
    - grep -q "tenant/dashboard" src/app/\(tenant\)/tenant/layout.tsx
    - grep -q "force-dynamic" src/app/\(landlord\)/landlord/layout.tsx
    - grep -q "force-dynamic" src/app/\(tenant\)/tenant/layout.tsx
  </acceptance_criteria>
  <done>Both layout files exist, compile without errors, use correct auth guards allowing broker/admin access, render PortalSidebar with role-specific nav items, follow same structure as broker portal layout</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Both route group directories exist: `src/app/(landlord)/landlord/` and `src/app/(tenant)/tenant/`
- PortalSidebar is a reusable component (not hardcoded to one portal)
- Landlord layout uses landlord-specific auth and nav items
- Tenant layout uses tenant-specific auth and nav items
- Both allow broker/admin access per project decision
</verification>

<success_criteria>
- PortalSidebar component exported from src/components/layout/portal-sidebar.tsx
- Landlord layout at src/app/(landlord)/landlord/layout.tsx with requireRole guard
- Tenant layout at src/app/(tenant)/tenant/layout.tsx with requireRole guard
- TypeScript compilation passes
- Visual design matches existing broker portal sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/05-landlord-and-tenant-portals/05-01-SUMMARY.md`
</output>
